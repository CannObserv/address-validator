# Seam hits judged legitimate. One entry per judged line; an entry stops
# matching the moment the line it acknowledges changes — which is when it
# needs re-judging. See curating-context Phase 6.5.

# Names the issue that introduced the behaviour the section documents, not
# provenance of a curation move. Pre-dates the #191 curation; the anchor is
# referenced from PR and issue prose.
docs/VALIDATION-PROVIDERS.md :: Pipeline-version invalidation (#145)

# Same shape, judged 2026-08-27: each names the issue that SETTLED the
# behaviour the section documents — the worktree-bootstrap half (#203) and the
# venv-knob decision (#201) — not provenance of a curation move. Both anchors
# are cited from the merged PR prose, and #201's heading carries the
# do-not-re-litigate marker that is the section's whole point.
docs/DEPLOYMENT.md :: Why `bash .skills/doctor.sh` is the other half (GH #203)
docs/DEPLOYMENT.md :: Why `none` and not `link` (GH #201 — settled, do not re-litigate)

# Legitimate navigation, both restored during the #191 review after an earlier
# pass cleared them by deleting the reference — the anti-pattern the skill's own
# rationalization table names. Port assignments are still in AGENTS.md
# § Infrastructure. The tool table moved to docs/SOCRATICODE.md in the #193
# init-socraticode v1.4 split, and the SKILLS.md line was rewritten to name both
# halves rather than acknowledged in place.
docs/SKILLS.md :: and `AGENTS.md` → Infrastructure for port assignments
docs/SKILLS.md :: `AGENTS.md` → Code Exploration Policy carries the negative rule.

# The block↔doc seam that init-socraticode v1.4's split creates by design: the
# policy block in AGENTS.md carries the rule, docs/SOCRATICODE.md carries the
# table, and each names the other. All four targets exist as written — the
# `## Code Exploration Policy` block and the `## Code Exploration Notes
# (repo-specific)` section, both in AGENTS.md.
docs/SOCRATICODE.md :: Detail doc for the `## Code Exploration Policy` block in [AGENTS.md](../AGENTS.md).
docs/SOCRATICODE.md :: heading down there, not in `AGENTS.md`.
docs/SOCRATICODE.md :: `AGENTS.md` block should be on its degraded variant.

# The skills index naming the file that skill's whole job is to curate. Nothing
# moved; the reference is the row's content.
docs/SKILLS.md :: Curates AGENTS.md + its doc tree against the token budget

# Naming AGENTS.md as one of the manifest's 11 registered artifacts, which it
# is. Not a cross-reference to relocated content.
docs/SOCRATICODE.md :: AGENTS.md, the two vendored USPS OpenAPI

# Re-judged 2026-09-23 (GH #221): the init-socraticode template re-wrapped
# both lines above; same claims, same targets. Plus the template's own advice
# not to park the rebuild rule in AGENTS.md — a statement about what belongs
# there, not a pointer to moved content.
docs/SOCRATICODE.md :: it. That rule does not need to live in your `AGENTS.md`

# GH #221 repo notes naming AGENTS.md content that is still there: the
# "dated snapshots" line (Detail Docs footer) and the #218 routing bullets that
# lead `## Code Exploration Notes (repo-specific)`. The pointer is the point —
# option E holds only while that routing stays in AGENTS.md.
docs/SOCRATICODE.md :: `.socraticodeignore` drops both — the directories AGENTS.md calls "dated
docs/SOCRATICODE.md :: first bullet of AGENTS.md's *Code Exploration Notes*). That routing is what
docs/SOCRATICODE.md :: makes E hold. Do not drop it from AGENTS.md without reopening #218.
# GH #223 split DEPLOYMENT.md "## Host memory" out to docs/HOST-MEMORY.md. These
# point at DEPLOYMENT.md content that did NOT move (Server lifecycle, and the two
# worktree rationale sections the doctor/venv-knob messages cite), or — the
# session-pin test — already read the moved section from its new doc.
docs/HOST-MEMORY.md :: `Service unit change` row under [Server lifecycle](DEPLOYMENT.md#server-lifecycle)
tests/unit/test_doctor_escape_hatch.py :: "See docs/DEPLOYMENT.md -> 'Why `bash .skills/doctor.sh` is the other half'."
tests/unit/test_worktree_venv_knob.py :: "See docs/DEPLOYMENT.md -> 'Why `none` and not `link`' (GH #201)."
tests/unit/test_worktree_venv_knob.py :: "see docs/DEPLOYMENT.md (GH #201)."
tests/unit/test_socraticode_session_pin.py :: SECTION_HEADING = "## Don't install a SocratiCode server at launch"
