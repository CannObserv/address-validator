# Judged warrants for lines a curation rewrote rather than moved (curating-context Phase 6).
# 2026-09-23, GH #221: class-C tightenings to bring AGENTS.md under 6,000 tokens; --claims: 0 atoms dropped.
AGENTS.md :: tighten :: - `unresolvedPct` is a **call**-edge statistic, not imports — import edges probe
AGENTS.md :: tighten :: exact, so an empty `codebase_graph_query`/`codebase_impact` answer means no
AGENTS.md :: tighten :: dependents, not a lossy graph. Measured yield, evidence and re-measurement
AGENTS.md :: tighten :: recipe: [docs/SOCRATICODE.md](docs/SOCRATICODE.md) → Repo-specific notes.
AGENTS.md :: tighten :: - New worktree bootstrap: `uv sync` **and** `bash .skills/doctor.sh` (~2 s) — no `.venv` is linked in, and every vendored skill/hook symlink dangles until the doctor initializes submodules ([docs/DEPLOYMENT.md](docs/DEPLOYMENT.md))
AGENTS.md :: tighten :: Worktrees get **no** `.venv` (`.skills/worktree_venv=none`) — run `uv sync` in a new worktree before its first test run (~0.2 s, ~4 MiB). This is why: under the `link` default a worktree's `uv sync` *prunes* the venv the port-8000 service runs from. Rationale and measurements → [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md).
AGENTS.md :: tighten :: | `socraticode:codebase-exploration` | Semantic search, dependency graphs — tool table in [docs/SOCRATICODE.md](docs/SOCRATICODE.md) |
AGENTS.md :: tighten :: | `socraticode:codebase-management` | Index management, health checks, file watching — see [docs/SOCRATICODE.md](docs/SOCRATICODE.md) |
# CR 2 on PR #222: the index line claimed SOCRATICODE.md holds the prefetch query;
# test_doc_points_at_the_hook_instead_of_copying_it finds zero select: copies there.
AGENTS.md :: disproven :: - [docs/SOCRATICODE.md](docs/SOCRATICODE.md) — `codebase_*` tool table, prefetch query, graph health
# 2026-09-24, GH #223: DEPLOYMENT.md's "## Host memory" split out to docs/HOST-MEMORY.md; these pointers
# were rewritten because what they point at moved in the same change.
docs/DEPLOYMENT.md :: retarget :: # MemoryLow= is inert without the system.slice drop-in (see Host memory below).
docs/DEPLOYMENT.md :: retarget :: | Service reservation — `MemoryLow=512M`, `OOMScoreAdjust=-500` | `infra/address-validator.service` | `Service unit change` row under [Server lifecycle](#server-lifecycle) |
AGENTS.md :: retarget :: - **No swap**; a session can't be OOM-killed, so the *service* dies instead — never run a big install unreserved → [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) → Host memory
AGENTS.md :: retarget :: - [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) — units, timers, DB scripts, env, worktree + dev-server, host memory
